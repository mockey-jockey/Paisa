import React from 'react';
import { View } from 'react-native';
import header from '../styles/header';

const HeaderView = (props) => {
    return <View style={header.headerViewStyle} />
}

export default HeaderView;